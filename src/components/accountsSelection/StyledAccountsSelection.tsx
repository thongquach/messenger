import { Space } from 'antd';
import styled from 'styled-components';

export const StyledAccountsContainer = styled(Space)`
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export const StyledAccountInfo = styled.div`
  display: flex;
  flex-direction: column;
  text-align: left;
`;
